package com.example.localloopapplication;

// Admin class extends User class, inheriting all properties and methods of User
// This allows Admin to have all User functionality plus additional admin-specific features (if added later)
public class Admin extends User {
    // Default constructor for Admin
    public Admin() {}
}
